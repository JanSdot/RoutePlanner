# Kopieren als .env.local fuer lokale Overrides, oder als Render-Umgebungsvariable setzen.
# Ohne diese Variable faellt api.ts auf http://localhost:5080 zurueck.
VITE_API_BASE_URL=https://your-api-service.onrender.com
